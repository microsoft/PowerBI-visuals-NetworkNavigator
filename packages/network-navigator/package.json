{
  "name": "@essex/network-navigator",
  "version": "1.0.1",
  "description": "",
  "main": "src/index.js",
  "typings": "src/index.d.ts",
  "scripts": {
    "clean": "rimraf dist",
    "lint": "tslint \"src/**/*[!.d].{ts,tsx}\"",
    "karma:tdd": "karma start --tdd",
    "karma:once": "karma start",
    "docs": "typedoc --out docs --ignoreCompilerErrors ./ --externalPattern \"**/node_modules/**/*.*\" --exclude \"**/src/**/*.spec.ts\"",
    "verify": "npm run clean && tsc && npm-run-all lint karma:once docs",
    "test": "npm run verify",
    "version": "npm run verify"
  },
  "author": "Microsoft",
  "license": "MIT",
  "dependencies": {
    "d3": "3.5.12",
    "debug": "^2.6.0",
    "jquery": "^2.2.0",
    "lodash": "4.0.0",
    "ts-loader": "^2.3.2"
  },
  "devDependencies": {}
}
