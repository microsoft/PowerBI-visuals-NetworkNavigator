{
  "scripts": {
    "postinstall": "lerna exec --concurrency=1 -- yarn && lerna exec --concurrency=99 -- linklocal"
  },
  "devDependencies": {
    "lerna": "2.0.0-beta.34",
    "linklocal": "^2.8.0"
  }
}
